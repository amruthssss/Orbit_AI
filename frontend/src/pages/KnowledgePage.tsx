/** Knowledge page composition seam. */
import { EmptyState } from "../components/common";

export function KnowledgePage() {
  return <EmptyState title="Knowledge base" description="Document features are composed by the existing workspace." />;
}
